services:
  omnistream:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: omnistream
    ports:
      - "${PORT}:8080"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - omnistream-network

networks:
  omnistream-network:
    driver: bridge
